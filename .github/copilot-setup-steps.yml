steps:
  - name: Setup Node.js
    uses: actions/setup-node@v4
    with:
      node-version: "20"
      cache: "npm"

  - name: Install dependencies
    run: npm ci

  - name: Verify build
    run: npm run build

  - name: Install Playwright browser for MCP
    run: npx playwright install --with-deps chromium
